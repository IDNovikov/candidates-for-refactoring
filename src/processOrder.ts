//необходимо добавить провеpку item
function processOrder(item: unknown, isExpress: boolean):void {
  if(item){
  // Логика обработки заказа с учетом экспресс-доставки
  if (isExpress) {
    // Логика для экспресс-доставки
  } else {
    // Логика для стандартной доставки
  }
  }

}
